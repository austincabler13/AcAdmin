export const kickPlayer = async (playerId: string) => {
    // Logic to kick player from the server
    console.log(`Kicking player with ID: ${playerId}`);
    return true;
  };
  
  export const banPlayer = async (playerId: string) => {
    // Logic to ban player from the server
    console.log(`Banning player with ID: ${playerId}`);
    return true;
  };
  