export const getServerStatus = async () => {
    // Fetch real-time server status data (stubbed example)
    const status = {
      playerCount: 12,
      uptime: "4h 32m",
      cpuUsage: "45%",
      memoryUsage: "2.3 GB",
    };
    return status;
  };
  